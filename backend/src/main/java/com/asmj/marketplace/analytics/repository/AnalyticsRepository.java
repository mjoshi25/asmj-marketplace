package com.asmj.marketplace.analytics.repository;import com.asmj.marketplace.analytics.model.AnalyticsEvent;import org.springframework.data.mongodb.repository.MongoRepository;import org.springframework.data.mongodb.repository.Aggregation;import java.util.*;public interface AnalyticsRepository extends MongoRepository<AnalyticsEvent,String>{long countByEvent(String event);long countByVendorIdAndEvent(String vendorId,String event);long countByVendorId(String vendorId);long countByPostId(String postId);}
