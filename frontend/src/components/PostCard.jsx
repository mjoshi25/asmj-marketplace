import {Link}from"react-router-dom";import{MapPin,Star,ArrowUpRight}from"lucide-react";
export default function PostCard({post}){const image=post.images?.[0],id=post.id||post._id;return <Link to={`/listing/${id}`} className="card card-hover group overflow-hidden"><div className="relative aspect-[4/3] overflow-hidden bg-slate-100">{image?<img src={image} alt={post.title} className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/>:<div className="grid h-full place-items-center text-slate-300">ASMJ</div>}<div className="absolute left-3 top-3 rounded-full bg-white/90 px-2.5 py-1 text-[10px] font-black uppercase tracking-wide text-slate-700 shadow-sm">{post.type||"LISTING"}</div><div className="absolute right-3 top-3 grid h-8 w-8 place-items-center rounded-full bg-white/90 text-slate-700 opacity-0 shadow-sm transition group-hover:opacity-100"><ArrowUpRight size={15}/></div></div><div className="p-4 sm:p-5"><h3 className="line-clamp-1 font-black text-slate-950">{post.title}</h3><p className="mt-1 line-clamp-2 min-h-[2.75rem] text-sm leading-5 text-slate-500">{post.description||"Discover this listing on ASMJ."}</p><div className="mt-4 flex items-center justify-between gap-2"><span className="font-black text-slate-950">{post.price!=null?`₹${Number(post.price).toLocaleString("en-IN")}`:"Contact vendor"}</span><span className="flex items-center gap-1 text-xs font-semibold text-slate-500"><Star size={13}/> {post.rating||"New"}</span></div>{post.location?.city&&<div className="mt-2 flex items-center gap-1 text-xs text-slate-400"><MapPin size={13}/>{post.location.city}</div>}</div></Link>}
