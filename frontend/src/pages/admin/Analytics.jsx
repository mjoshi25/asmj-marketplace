import{useEffect,useState}from"react";import{getAnalytics}from"../../api/adminApi";export default function Analytics(){const[x,setX]=useState({});useEffect(()=>{getAnalytics().then(r=>setX(r.data?.data||{})).catch(()=>{})},[]);return <div className="page-shell admin-bg"><p className="eyebrow">ADMIN · V16</p><h1 className="page-title">Platform analytics</h1><p className="mt-2 text-sm text-slate-500">Marketplace activity plus transaction and recruitment workflow metrics.</p><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{Object.entries(x).map(([k,v])=><div className="card p-5" key={k}><p className="text-xs font-bold uppercase tracking-wider text-slate-400">{k.replace(/([A-Z])/g," $1")}</p><p className="mt-2 text-3xl font-black">{typeof v==="number"?v.toLocaleString("en-IN"):v}</p></div>)}</div></div>}