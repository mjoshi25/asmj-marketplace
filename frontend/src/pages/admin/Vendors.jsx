import{useEffect,useState}from"react";import{getVendors,approveVendor,rejectVendor}from"../../api/adminApi";export default function Vendors(){const[x,setX]=useState([]);const load=()=>getVendors().then(r=>setX(r.data?.data||[])).catch(()=>setX([]));useEffect(()=>{load()},[]);const act=async(v,ok)=>{if(ok)await approveVendor(v.id);else await rejectVendor(v.id,"Rejected by admin");load()};return <div className="mx-auto max-w-7xl px-4 py-8"><p className="text-sm text-slate-400">ADMIN</p><h1 className="text-3xl font-black">Vendors</h1><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{x.map(v=><div className="card p-5" key={v.id}><b>{v.businessName}</b><p className="text-sm text-slate-500">{v.businessType}</p><p className="mt-3 text-xs">{v.verificationStatus}</p><div className="mt-4 flex gap-2"><button className="btn-primary" onClick={()=>act(v,true)}>Approve</button><button className="btn-secondary" onClick={()=>act(v,false)}>Reject</button></div></div>)}</div></div>}
