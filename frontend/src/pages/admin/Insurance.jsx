import{useEffect,useState}from"react";import{getAdminInsurancePlans,approveInsurancePlan,getAdminInsuranceQuotes}from"../../api/insuranceApi";export default function Insurance(){const[p,setP]=useState([]),[q,setQ]=useState([]),[m,setM]=useState("");const load=()=>{getAdminInsurancePlans().then(r=>setP(r.data?.data||[]));getAdminInsuranceQuotes().then(r=>setQ(r.data?.data||[]))};useEffect(() => { load(); }, []);const act=async(id,a)=>{try{await approveInsurancePlan(id,a);setM(a?"Insurance plan approved and published":"Insurance plan rejected");load()}catch(e){setM(e.response?.data?.message||"Action failed")}};return <div className="page-shell admin-bg"><p className="eyebrow">ADMIN · V25</p><h1 className="page-title">Insurance control</h1>{m&&<div className="mt-4 rounded-xl bg-slate-900 p-3 text-sm text-white">{m}</div>}<div className="mt-6 card overflow-hidden"><div className="divide-y divide-slate-100">{p.map(x=><div className="p-5" key={x.id}><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><b>{x.planName}</b><p className="text-sm text-slate-500">{x.insurerName} · {x.insuranceType} · ₹{Number(x.premium).toLocaleString("en-IN")}/{x.premiumFrequency||"period"}</p></div><div className="flex gap-2"><button className="btn-primary" onClick={()=>act(x.id,true)}>Approve</button><button className="btn-secondary text-red-600" onClick={()=>act(x.id,false)}>Reject</button></div></div></div>)}{!p.length&&<div className="p-12 text-center text-slate-400">No insurance plans.</div>}</div></div><div className="card mt-6 p-5"><h2 className="section-title">Quote requests</h2><div className="mt-4 divide-y">{q.map(x=><div className="py-3" key={x.id}><b>{x.quoteNumber}</b><p className="text-sm text-slate-500">{x.customerName} · {x.status} · ₹{Number(x.estimatedPremium||0).toLocaleString("en-IN")}</p></div>)}</div></div></div>}
