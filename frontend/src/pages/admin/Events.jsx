import{useEffect,useState}from"react";import{getAdminEvents,approveEvent}from"../../api/eventApi";export default function Events(){const[d,setD]=useState([]),[m,setM]=useState("");const load=()=>getAdminEvents().then(r=>setD(r.data?.data||[]));useEffect(() => { load(); }, []);const act=async(id,a)=>{try{await approveEvent(id,a);setM(a?"Event approved and published":"Event rejected");load()}catch(e){setM(e.response?.data?.message||"Action failed")}};return <div className="page-shell admin-bg"><p className="eyebrow">ADMIN · V24</p><h1 className="page-title">Event approvals</h1>{m&&<div className="mt-4 rounded-xl bg-slate-900 p-3 text-sm text-white">{m}</div>}<div className="mt-6 card overflow-hidden"><div className="divide-y divide-slate-100">{d.map(x=><div className="p-5" key={x.id}><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><b>{x.name}</b><p className="text-sm text-slate-500">{x.eventDate} · {x.venue}, {x.city} · {x.availableSeats}/{x.capacity} seats · ₹{Number(x.ticketPrice).toLocaleString("en-IN")}</p></div><div className="flex gap-2"><button className="btn-primary" onClick={()=>act(x.id,true)}>Approve</button><button className="btn-secondary text-red-600" onClick={()=>act(x.id,false)}>Reject</button></div></div></div>)}{!d.length&&<div className="p-12 text-center text-slate-400">No events available.</div>}</div></div></div>}
