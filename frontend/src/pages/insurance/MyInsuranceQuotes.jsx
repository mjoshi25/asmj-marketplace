import{useEffect,useState}from"react";import{getMyInsuranceQuotes}from"../../api/insuranceApi";export default function MyInsuranceQuotes(){const[d,setD]=useState([]);useEffect(()=>{getMyInsuranceQuotes().then(r=>setD(r.data?.data||[]))},[]);return <div className="page-shell user-bg"><p className="eyebrow">INSURANCE · V25</p><h1 className="page-title">My quote requests</h1><div className="mt-6 space-y-4">{d.map(x=><div className="card p-5" key={x.id}><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><div><b>{x.quoteNumber}</b><p className="mt-1 text-sm text-slate-500">{x.customerName} · {x.members||1} member(s) · {x.city||""}</p><p className="mt-1 text-xs text-slate-400">Estimated premium: ₹{Number(x.estimatedPremium||0).toLocaleString("en-IN")}</p></div><span className="status-pill bg-slate-100 text-slate-700">{x.status}</span></div>{x.vendorNotes&&<p className="mt-3 rounded-lg bg-slate-50 p-3 text-sm text-slate-600">Vendor: {x.vendorNotes}</p>}</div>)}{!d.length&&<div className="card p-12 text-center text-slate-400">No quote requests yet.</div>}</div></div>}
