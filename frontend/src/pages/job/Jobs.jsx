import{useEffect,useState}from"react";import{getJobs}from"../../api/jobApi";import{BriefcaseBusiness,Clock3,RefreshCw,IndianRupee}from"lucide-react";
export default function Jobs(){const[data,setData]=useState([]),[q,setQ]=useState(""),[loading,setLoading]=useState(true);const load=()=>{setLoading(true);getJobs().then(r=>setData(r.data?.data||[])).finally(()=>setLoading(false))};useEffect(() => { load(); }, []);const list=data.filter(x=>`${x.title} ${x.locationText||""} ${(x.skills||[]).join(" ")}`.toLowerCase().includes(q.toLowerCase()));return <div className="page-shell user-bg"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow">CAREERS · V22</p><h1 className="page-title">Find your next opportunity</h1><p className="mt-2 text-sm text-slate-500">Approved jobs from ASMJ employers.</p></div><button className="btn-secondary" onClick={load}><RefreshCw size={16}/> Refresh</button></div><input className="input mt-6" placeholder="Search title, location or skill" value={q} onChange={e=>setQ(e.target.value)}/>{loading?<div className="card mt-6 p-12 text-center text-slate-400">Loading jobs…</div>:<div className="mt-6 grid gap-4 lg:grid-cols-2">{list.map(x=><div className="card p-5" key={x.id}><div className="flex gap-4"><div className="grid h-14 w-14 shrink-0 place-items-center rounded-2xl bg-indigo-50 text-indigo-600"><BriefcaseBusiness/></div><div className="min-w-0 flex-1"><h2 className="text-lg font-black">{x.title}</h2><p className="mt-1 text-sm text-slate-500">{x.locationText||"Location flexible"} · {x.employmentType||"Employment"} · {x.workMode||"Flexible"}</p><div className="mt-3 flex flex-wrap gap-3 text-xs font-bold text-slate-500"><span><Clock3 size={13} className="inline"/> {x.experienceLevel||"Experience"}</span>{x.minSalary!=null&&<span><IndianRupee size={13} className="inline"/>{Number(x.minSalary).toLocaleString("en-IN")}–{Number(x.maxSalary||x.minSalary).toLocaleString("en-IN")}/{x.salaryPeriod||"year"}</span>}</div><div className="mt-3 flex flex-wrap gap-2">{(x.skills||[]).slice(0,6).map(s=><span className="status-pill bg-slate-100 text-slate-600" key={s}>{s}</span>)}</div><a className="btn-primary mt-4 inline-flex" href={`/listing/${x.postId}`}>View & apply</a></div></div></div>)}{!list.length&&<div className="card p-12 text-center text-slate-500 lg:col-span-2">No matching jobs.</div>}</div>}</div>}
