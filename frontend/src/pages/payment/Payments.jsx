import{useEffect,useState}from"react";import{Link}from"react-router-dom";import{Upload,FileText,ExternalLink}from"lucide-react";import{getMyPayments,getMyInvoices}from"../../api/paymentApi";
export default function Payments(){const[p,setP]=useState([]),[i,setI]=useState([]);useEffect(()=>{getMyPayments().then(r=>setP(r.data?.data||[]));getMyInvoices().then(r=>setI(r.data?.data||[]))},[]);return <div className="page-shell user-bg"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow">PAYMENTS · V26</p><h1 className="page-title">Payments & invoices</h1></div><Link className="btn-primary" to="/payments/upload"><Upload size={16}/> Upload payment proof</Link></div><section className="mt-7"><h2 className="section-title">My payment records</h2><div className="mt-4 space-y-3">{p.map(x=><div className="card p-5" key={x.id}><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><div className="font-black">{x.transactionNumber}</div><p className="text-sm text-slate-500">{x.paymentType} · {x.bookingType} · {x.bookingId}</p><p className="mt-1 text-xs text-slate-400">Ref: {x.paymentReference} · {x.paymentMethod}</p></div><div className="text-left sm:text-right"><div className="text-xl font-black">₹{Number(x.amount).toLocaleString("en-IN")}</div><span className="status-pill bg-slate-100 text-slate-700">{x.status}</span>{x.screenshotUrl&&<a className="mt-2 block text-xs font-bold text-slate-600" href={x.screenshotUrl} target="_blank" rel="noreferrer">View proof <ExternalLink size={12} className="inline"/></a>}</div></div></div>)}{!p.length&&<div className="card p-10 text-center text-slate-400">No payment records.</div>}</div></section><section className="mt-9"><h2 className="section-title">Issued invoices</h2><div className="mt-4 space-y-3">{i.map(x=><div className="card p-5" key={x.id}><div className="flex items-center justify-between gap-3"><div><div className="font-black">{x.invoiceNumber}</div><p className="text-sm text-slate-500">{x.invoiceType} · {x.bookingType} · {x.bookingId}</p></div><div className="text-right"><div className="font-black">₹{Number(x.amount).toLocaleString("en-IN")}</div><span className="text-xs font-bold text-slate-500">{x.status}</span></div></div></div>)}{!i.length&&<div className="card p-10 text-center text-slate-400">Invoices are issued after payment proof is verified.</div>}</div></section></div>}
